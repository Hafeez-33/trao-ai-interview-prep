# Optional in local development (defaults to /api/v1 via Vite proxy)
# Production: Set to the full URL of the deployed backend API on Render (e.g. https://<backend-service>.onrender.com/api/v1)
VITE_API_BASE_URL=
